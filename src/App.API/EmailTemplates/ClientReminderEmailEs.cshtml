@model App.Application.DTOs.ReminderEmailModel

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Appointment Confirmation</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background-color: #f0f2f5;
            margin: 0;
            padding: 0;
        }

        .container {
            background-color: #ffffff;
            margin: 50px auto;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            text-align: center;
        }

        h2 {
            color: #333333;
            font-size: 28px;
            margin-bottom: 20px;
        }

        p {
            color: #666666;
            font-size: 16px;
            line-height: 1.6;
        }

        .highlight {
            color: #4CAF50;
            font-weight: bold;
        }

        .footer {
            margin-top: 30px;
            font-size: 14px;
            color: #999999;
        }

        .button {
            display: inline-block;
            padding: 12px 24px;
            font-size: 16px;
            color: #ffffff;
            background-color: #4CAF50;
            border-radius: 6px;
            text-decoration: none;
            margin: 20px 10px;
            transition: background-color 0.3s ease;
        }

            .button:hover {
                background-color: #45A049;
            }

        .contact-info {
            margin: 20px 0;
        }

            .contact-info p {
                margin: 5px 0;
            }

        .map-button {
            margin-top: 20px;
        }

        .button-icon {
            display: flex;
            align-items: center;
            justify-content: center;
        }

            .button-icon img {
                margin-right: 8px;
            }

        .social-icons {
            margin-top: 20px;
        }

            .social-icons a {
                display: inline-block;
                margin: 0 10px;
            }

            .social-icons img {
                width: 24px;
                height: 24px;
            }
    </style>
</head>
<body>
    <div class="container">
        <h2>Recordatorio de cita para el <span class="highlight">@Model.ReservationDate.ToString("MMMM dd, yyyy")</span> a las <span class="highlight">@Model.ReservationDate.ToString("hh:mm tt")</span></h2>
        <p>Estimado/a <span class="highlight">@Model.ClientName</span>,</p>
        <p>Este es un recordatorio de 1 hora de su cita confirmada el <span class="highlight">@Model.ReservationDate.ToString("MMMM dd, yyyy")</span> a las <span class="highlight">@Model.ReservationDate.ToString("hh:mm tt")</span> en nuestro estudio. Si tiene alguna pregunta, por favor póngase en contacto con nosotros en:</p>
        <div class="contact-info">
            <p class="highlight">@Model.PhoneNumber</p>
            <p class="highlight">@Model.Address</p>
        </div>
        <p>¡Gracias por elegirnos y esperamos verle pronto!</p>
        <p>Saludos cordiales,</p>
        <p><strong>@Model.ProviderName</strong></p>
        <a href="tel:@Model.PhoneNumber" class="button button-icon"><img src="css/images/phone.png" alt="Icono de Teléfono" /> Llámanos</a>
        <div class="social-icons">
            <a href="@Model.Facebook" target="_blank"><img src="css/images/facebook-new.png" alt="Icono de Facebook" /></a>
            @* <a href="@Model.TikTok" target="_blank"><img src="css/images/tiktok.png" alt="Icono de TikTok" /></a>
            <a href="@Model.Instagram" target="_blank"><img src="css/images/instagram-new.png" alt="Icono de Instagram" /></a> *@
        </div>
        <div class="footer">
            <p>Si tiene alguna pregunta, por favor contáctenos con la información proporcionada arriba.</p>
        </div>
    </div>
</body>
</html>
